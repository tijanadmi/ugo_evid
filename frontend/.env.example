# Go backend; Vite forwards /api requests here during development.
VITE_BACKEND_URL=http://localhost:8080
# For production, route /api to the backend or set its full URL before building.
VITE_API_BASE_URL=/api
